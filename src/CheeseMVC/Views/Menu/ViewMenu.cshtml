@model CheeseMVC.ViewModels.ViewMenuViewModel

@{ 
    ViewBag.Title = Model.Menu.Name;
}
<h1>@Model.Menu.Name Menu</h1>

<ul>
    @foreach (var cheeseMenu in Model.Items)
    {
        <!--In instructions: at_cheeseMenu.Cheeses.Name-->
        <li>@cheeseMenu.Cheese.Name</li>

    }
</ul>

@if (Model.Items.Count == 0)
{
    <p><i>No menu items, yet!</i></p>
}

<p><a asp-controller="Menu" asp-action="AddItem" asp-route-id="@Model.Menu.ID">Add Menu Item</a></p>